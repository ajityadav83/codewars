function findOdd(A) {
  for (var i = 0; i < A.length; i++) {
    var  count = 0;
    for (var j =0; j < A.length; j++  ){
      if ( A[j]==A[i]){
        count++;
      }
    }
    var even=0;
    even = count%2;
    if (even !==0 ){
      return A[i];
    }
  }
}
