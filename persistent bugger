function persistence(num) {
var sum =1 ;
var count = 0;
if (Math.floor (num / 10) >0){
  while (num){
    sum =1 ;
    count++;
     while(num){
    sum *= num % 10;
    num = Math.floor(num / 10);
   
    }
    if (Math.floor (sum / 10) >0){
          num =sum;
    }
  }
  }
  return count;
}
