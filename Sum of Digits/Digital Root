function digital_root(num) {
var sum =0 ;
var count = 0;
if (Math.floor (num / 10) >0){
  while (num){
    sum =0 ;
    count++;
     while(num){
    sum += num % 10;
    num = Math.floor(num / 10);
   
    }
    if (Math.floor (sum / 10) >0){
          num =sum;
    }
  }
  }
  return sum;
}
